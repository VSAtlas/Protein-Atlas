1. install all the softwares required 
2. open miniconda and copy these commands in 
conda create -n docking-env python=3.10 -y
conda activate docking-env
conda install -c conda-forge rdkit openbabel tqdm -y
pip install pandas biopython p2ranktools
pip install pywin32
pip install biopython
conda install requests
conda install -c conda-forge pubchempy
conda install -c conda-forge "rdkit>=2022.09"
conda install -c conda-forge rdkit
 
conda install -c salilab modeller
set MODELLER_KEY=MODELIRANJE   # on Windows, also this might not work, might have to manually edit the modeller config file
export MODELLER_KEY=MODELIRANJE # on Linux/macOS
config should be in a spot like this E:\miniconda3\envs\docking-env\Library\modeller\modlib\modeller

if you get this in the ouput 
Sorry: chem_data/rotarama_data/*.pickle files are missing or out of date.
Please run
    mmtbx.rebuild_rotarama_cache
to resolve this problem.

Run this in CMD E:\phenix\Library\bin\phenix.python.bat -m mmtbx.command_line.rebuild_rotarama_cache
also this is probably a good idea too E:\phenix\Library\bin\phenix.python.bat -m mmtbx.command_line.rebuild_cablam_cache




3. change interpreter to miniconda C:\Users\<YourUsername>\miniconda3\envs\docking-env\python.exe

4. 
TO GET PHENIX working, download this version Phenix-1.21.2-5419-Windows-x86_64.exe, it is the windows graphical installer
NOTE may need permission for academic purposes, I can send the phenix version I have or Im sure theyd approve us if we explained
they approved mine automatically but that might be because I still have my ut email

also copy and paste this file reduce_wwPDB_het_dict.txt into E:\phenix\Library\bin 


5. edit config 